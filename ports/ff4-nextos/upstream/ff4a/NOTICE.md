# Third-party notices

This port's host runtime is GPL-3.0-only. The game itself is never distributed
here: Final Fantasy IV, its engine, its OBB, its art and its audio are the
property of Square Enix and are supplied by the player from a legally owned
copy.

## NXExtract

The installer bundled under `nxextract/` is NXExtract, MIT licensed. See
`licenses/NXExtract-MIT.txt`.

## stb

`stb_image.h` and `stb_truetype.h` by Sean Barrett are in the public domain
(alternatively MIT). Their license text is reproduced at the top of each file
under `src/`.

## SDL2, zlib

Supplied by the device firmware or by PortMaster; not redistributed here.
