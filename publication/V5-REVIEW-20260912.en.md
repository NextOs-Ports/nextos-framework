# V5 publication tooling review

[Português](V5-REVIEW-20260912.md)

This review fixes tools in the collection prepared for publication. It keeps the V5 runtime, NXExtract, interfaces, native examples and port snapshots frozen. V6 was not incorporated. The repository remains private.

## Implemented fixes

| Boundary | Observed failure | Fix |
| --- | --- | --- |
| Frozen selection | Accepted additional files and executable-bit changes | Requires the exact set, hashes and executable modes, without symlinks |
| V5 identity | Changing runtime and its hash in the manifest itself could pass | Also pins the V5 export manifest SHA-256 |
| Workspace | Force-added Git data in ignored directories could escape | Rejects tracked content in `work/`, `build/`, `inputs/`, caches and environment files |
| ELF inventory | ELF64 could appear as ARMv7 without a blocker | Checks class, machine, header version and size; flags requirements already rejected by V5 loader |
| Android set | Accepted duplicate bases/splits or conflicting known versions | Rejects duplicate identities and conflicting versions; preserves unknown fields |
| Limits | Applied 8 GiB per APK; decompressed manifest before checking 2 MiB | Sums all APKs and checks the manifest before reading |
| Recovery | Special characters changed the URL; provenance could be overwritten | Encodes the path, compares already pinned hashes and requires new files |

The new [V5 collection checks](../.github/workflows/collection.yml) workflow runs integrity, bilingual guides and tooling tests on push/PR. It uses only the collection and synthetic fixtures. [Full SDK training](../.github/workflows/onboarding.yml) remains manual. Neither workflow publishes commercial data or game artifacts.

## Follow-up: Android runtime guides

[Android runtime selection](../docs/en/ANDROID-RUNTIMES.md) connects eight tracks. PT/EN guides were added for GameMaker, Ren'Py, Haxe/hxcpp/Lime and native C/C++, covering bootstrap, ABI, reference files, extraction and diagnostics. MonoGame/FNA remains within the Android-build context.

Search defaults to Android source and accepts `--runtime`, retaining explicit historical queries with `--platform all`. Inventory recognizes the three new families only from inspected native libraries; asset files cannot impersonate runtimes. Fifteen new tests cover detection, filters, guide links, evidence hashes and preservation of support limits.

Seventeen classifications were clarified with paths and hashes from already selected snapshots. All 40 source repositories were checked as public on GitHub on 2026-09-12. No new game source was imported. Tightrope 1.0.5-test.1 retains pending acceptance; classification neither approves its bytes nor promotes its adjustment into V5.

## Evidence and limits

The initial review passed 48 host tests: 17 scope/integrity, 13 Android compatibility, 12 inventory/catalog and 6 recovery tests. Negative cases include 32/64-bit ELF, split versions, altered manifest, additional file, permissions, symlinks and a mocked URL. Mocked recovery does not prove external server availability. The workflow also runs the 15 tests added in the Android follow-up; consult the result bound to the final commit on GitHub.

The preserved manifest SHA-256 is `c038cd9bb1e9bfedc0f24a8b51639fccf5a464a3dfd474b950463315bcb59662`; its origin remains `framework-v5` / `657fb65a23b5c3b20040e76307b27e6470b1d17c`. The verifier checks the 1,285 export files and port selections. It examines the current tree and Git index paths without certifying the entire history or shim coverage.

SDK, ARM examples, NXExtract and games were not rebuilt or rerun. Earlier evidence retains its own bytes and limits in [ONBOARDING-UPDATE.en.md](ONBOARDING-UPDATE.en.md). No physical test, ZIP, release, migration or visibility change occurred.

## Items before publication

- Review licenses per file and decide terms for new prose while preserving existing notices.
- Decide which history to make public: private commits still contain excluded material. The pin helper depends on a historical commit; preparing a different history requires preserving provenance and validating pins before replacing its reference.
- Run full SDK training on GitHub and pending Unity, Cocos and separate NDK exercise builds.
- Physically validate graphical installation, NXSplash, image, sound, controls, save and exit for the example on authorized hardware.
- Recover the origin of FP2's historical SPIRV-Cross pin and bind support claims to exact artifact evidence.

Tracking remains in [REVIEW.en.md](REVIEW.en.md). Publishing this collection does not certify all games or devices; this review does not authorize changes to frozen interfaces or runtime.
